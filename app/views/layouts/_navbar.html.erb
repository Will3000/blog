<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand">Blog</a>
    </div>
    <ul class="nav navbar-nav">
      <li><a href="/"><span class="glyphicon glyphicon-home"></span> Home</a></li>
      <li><%= link_to("About",about_path) %></li>
      <li><%= link_to("Posts",posts_path(:page => 1)) %></li>
      <% if !session[:user_id] %>
        <li><%= link_to "Sign In", "#loginModal", "data-toggle" => "modal", :class => "btn"%></li>
        <li><%= link_to "Sign Up", "#signUpModal", "data-toggle" => "modal", :class => "btn"%></li>

      <% end %>
    </ul>
    <div class="right-nav">
      <% if session[:user_id] %>
        <div class="dropdown">
          <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">My account
          <span class="caret"></span></button>
          <ul class="dropdown-menu">
            <li><%= link_to "Profile", user_path(session[:user_id]) %></li>
            <li><%= link_to "My Favourites", user_favourites_path(current_user) %></li>
            <li><%= link_to "New Post", new_post_path %></li>
            <li><%= link_to "Sign Out", sessions_path, method: :delete %></li>
          </ul>
        </div>
      <% end %>
      <div class="search-bar">
        <%= form_tag root_path(page: 1), method: :get do %>
          <%= text_field_tag :search, params[:search] %>
          <button class='btn btn-primary' style='width:40px;'>
            <%= fa_icon("search") %>
          </button>
        <% end %>
      </div>
  </div><!-- .right-nav -->
  </div>
</nav>
