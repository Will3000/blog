<%= simple_form_for @post do |f| %>
  <%= f.input :title %>
  <%= f.input :body, as: :text, :cols => 10, :rows => 20%>
  <%= f.association :category %>
  <%= f.association :tags, as: :check_boxes %>
  <%= f.submit({class: "btn btn-primary"}) %>
<% end %>
