<h1>Create New Comment</h1>

<%= @comment.errors.full_messages.join(", ") %>
<%= form_for @comment do |f| %>
  <div>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div><br>
  <%= f.submit %>
<% end %>
